mutation offerMutation(
  $classTimeId: String,
  $comment: String,
	$enrollmentId: String,
) {
  createOffer(
    classTimeId: $classTimeId,
  	comment: $comment,
    enrollmentId: $enrollmentId
  ) {
  	offer {
      id
    }
  }
}
